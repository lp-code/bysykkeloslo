from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Analyse the trip data from the bicycle sharing scheme in Oslo.',
    author='Lars Pesch',
    license='',
)
